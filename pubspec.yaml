name: flutter_sms_example
description: Demonstrates how to use the flutter_sms plugin.
publish_to: 'none'

environment:
  sdk: '>=2.12.0-259.8.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  column_scroll_view: ^1.0.3  

dev_dependencies:
  flutter_sms:
    path: ../
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

